## Packages
framer-motion | Page transitions and subtle animations
date-fns | Date formatting for blog posts
lucide-react | Icons for UI elements

## Notes
Tailwind Config: Font family 'Barlow' needs to be added to tailwind.config.ts by the user or applied via CSS variables.
API: Projects content is JSONB, frontend needs to render specific sections (context, problem, etc).
